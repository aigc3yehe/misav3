import { Box, Typography } from '@mui/material';

export default function OldWorkstation() {
  return (
    <Box sx={{ backgroundColor: '#020300' }}>
      <Typography variant="h4">Workstation</Typography>
    </Box>
  );
} 